apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "legal-api.fullname" . }}-test-connection"
  labels:
    {{- include "legal-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "legal-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
